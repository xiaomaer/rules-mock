import Mock from './src/mock';

/**
 * 根据自定义的mock规则生成mock数据
 * @param {object} rules ：自定义的mock规则
 */
export default function (rules) {

}


/* 
新增方法：
1、执行函数返回结果，函数上下文为属性所在的对象
2、解析自定义mock规则，生成mock数据
 */
